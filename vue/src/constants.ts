export enum DescCellTypes {
  ROW = "ROW",
  COLUMN= "COLUMN",
  EMPTY = "EMPTY",
};

export enum Cell {
  NONE = "NONE",
  SELECT = "SELECT",
  EDIT = "EDIT",
};

export enum EditMode {
  NONE = '',
  SELECT = 'select',
  EDIT = 'edit',
}
